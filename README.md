 # 🧪 Sauce Demo Automation Tests

Автоматизированное тестирование логина на сайте [https://www.saucedemo.com](https://www.saucedemo.com) с использованием Python, Selenium, Pytest и Allure в Docker-окружении.

## 📋 Содержание
- [Техническое задание](#техническое-задание)
- [Архитектура проекта](#архитектура-проекта)
- [Быстрый старт](#быстрый-старт)
- [Запуск тестов](#запуск-тестов)
- [Структура проекта](#структура-проекта)
- [Документация](#документация)

---

## 🎯 Техническое задание

Проект реализует все требования из ТЗ:
- ✅ **5 тестовых сценариев** авторизации на сайте saucedemo.com
- ✅ **Page Object Pattern** для структурирования кода
- ✅ **Selenium WebDriver** для автоматизации браузера
- ✅ **Allure-отчёты** для визуализации результатов
- ✅ **Проверка корректности URL и отображения элементов**
- ✅ **Dockerfile** для запуска тестов в контейнере
- ✅ **Python 3.10** в качестве языка программирования
- ✅ **requirements.txt** со всеми зависимостями
- ✅ **README.md** с инструкциями по запуску

### Тестовые сценарии
1. **Успешный логин** - `standard_user / secret_sauce`
2. **Логин с неверным паролем** - проверка сообщения об ошибке
3. **Логин заблокированного пользователя** - `locked_out_user`
4. **Логин с пустыми полями** - валидация обязательных полей
5. **Логин пользователем с проблемами производительности** - `performance_glitch_user` с проверкой времени загрузки

---

## 🏗️ Архитектура проекта

Проект построен с использованием современных практик автоматизации тестирования:

### 📐 Page Object Pattern
- **`BasePage`** - базовый класс со стандартными методами работы с элементами
- **`LoginPage`** - объект страницы авторизации с локаторами и методами
- **`InventoryPage`** - объект страницы с товарами после успешного входа

### 🧪 Pytest Framework
- **Фикстуры** (`conftest.py`) для настройки и завершения работы драйвера
- **Параметризованные тесты** для проверки разных пользователей
- **Allure-аннотации** для детализированных отчётов

### 🐳 Docker Integration
- Изолированное окружение для стабильного запуска тестов
- Автоматическая установка Chrome и зависимостей
- Готовность к интеграции в CI/CD pipeline

---

## 🚀 Быстрый старт

### Предварительные требования
- **Python 3.10+** ([скачать](https://www.python.org/downloads/))
- **Git** ([скачать](https://git-scm.com/downloads))
- **Docker Desktop** ([скачать](https://www.docker.com/products/docker-desktop)) - для запуска в контейнере
- **Google Chrome** - для локального запуска тестов

### Клонирование репозитория
```bash
git clone https://github.com/ВАШ_НИК/sauce-demo-tests.git
cd sauce-demo-tests
```

### Установка зависимостей
```bash
pip install -r requirements.txt
```

---

## 🧪 Запуск тестов

### Локальный запуск (рекомендуется для разработки)

#### 1. Все тесты с подробным выводом
```bash
pytest src/tests/ -v
```

#### 2. С генерацией HTML-отчёта
```bash
pytest src/tests/ --html=report.html
# Открыть отчёт: open report.html
```

#### 3. С генерацией Allure-отчёта
```bash
# Запуск тестов с сохранением результатов
pytest src/tests/ --alluredir=allure-results

# Генерация и открытие отчёта (требуется установленный Allure)
allure generate allure-results -o allure-report --clean
allure open allure-report
```

#### 4. Запуск конкретных тестов
```bash
# Только 5 обязательных тестов из ТЗ
pytest src/tests/test_login_page.py -k "valid_credentials or invalid_password or locked_out or empty_fields or performance_glitch"

# Тест успешного логина
pytest src/tests/test_login_page.py::TestLogin::test_login_with_valid_credentials -v
```

### Запуск в Docker (полная изоляция)

#### 1. Сборка Docker-образа
```bash
docker build -t sauce-demo-tests .
```

#### 2. Запуск тестов в контейнере
```bash
docker run --rm sauce-demo-tests
```

#### 3. Запуск с сохранением отчётов
```bash
# Создайте папки для отчётов
mkdir -p allure-results reports

# Запустите контейнер с монтированием папок
docker run --rm \
  -v $(pwd)/allure-results:/app/allure-results \
  -v $(pwd)/reports:/app/reports \
  sauce-demo-tests
```

#### 4. Полный запуск через Docker Compose (все сервисы)
```bash
# Запуск Selenium, тестов и серверов отчётов одной командой
docker-compose up --build

# После запуска откройте:
# - Allure отчёт: http://localhost:5050
# - HTML отчёт: http://localhost:8080
```

---

## 📁 Структура проекта

```
sauce-demo-tests/
├── src/
│   ├── pages/                    # Page Objects
│   │   ├── __init__.py
│   │   ├── base_page.py         # Базовый класс страницы
│   │   ├── login_page.py        # Страница авторизации
│   │   └── inventory_page.py    # Страница товаров
│   └── tests/                   # Тестовые сценарии
│       ├── __init__.py
│       └── test_login_page.py   # 5+ тестов авторизации
├── config/                      # Конфигурация
│   └── config.py               # Константы и настройки
├── utils/                       # Вспомогательные функции
│   └── helpers.py
├── conftest.py                 # Pytest фикстуры
├── requirements.txt            # Зависимости Python
├── pytest.ini                 # Конфигурация Pytest
├── Dockerfile                 # Конфигурация Docker
├── docker-compose.yml         # Оркестрация контейнеров
├── docker-entrypoint.sh       # Скрипт запуска в контейнере
├── .gitignore                 # Игнорируемые файлы Git
└── README.md                  # Документация
```

### Ключевые файлы

#### `src/pages/login_page.py`
```python
class LoginPage(BasePage):
    USERNAME_FIELD = (By.ID, 'user-name')
    PASSWORD_FIELD = (By.ID, 'password')
    LOGIN_BUTTON = (By.ID, 'login-button')
    
    def login(self, username, password):
        self.enter_username(username)
        self.enter_password(password)
        self.click_login()
```

#### `src/tests/test_login_page.py`
```python
class TestLogin:
    @allure.title("Успешный логин стандартным пользователем")
    def test_login_with_valid_credentials(self, login_page):
        login_page.login(username="standard_user", password="secret_sauce")
        assert "inventory" in login_page.driver.current_url
```

#### `conftest.py`
```python
@pytest.fixture
def driver():
    service = Service(ChromeDriverManager().install())
    driver = webdriver.Chrome(service=service)
    yield driver
    driver.quit()
```

---

## 🔧 Конфигурация

### Переменные окружения
```bash
export BROWSER=chrome           # или firefox
export HEADLESS=true           # запуск без GUI
export TIMEOUT=10              # время ожидания элементов
```

### Пользователи для тестирования
Из документации saucedemo.com:
- `standard_user` - стандартный пользователь
- `locked_out_user` - заблокированный пользователь
- `problem_user` - пользователь с проблемами
- `performance_glitch_user` - пользователь с задержками
- `error_user` - пользователь с ошибками
- `visual_user` - пользователь для тестов верстки

**Пароль для всех пользователей:** `secret_sauce`

---

## 📊 Отчёты

### Allure Report
Интерактивный отчёт с:
- Детальной историей выполнения тестов
- Графиками и статистикой
- Прикреплёнными скриншотами (при наличии)
- Фильтрацией по тегам и статусам


### HTML Report
Статичный отчёт pytest-html:
- Быстрый просмотр результатов
- Простая навигация
- Минимальные зависимости

---

## 🐛 Устранение неполадок

### Частые проблемы и решения

| Проблема | Решение |
|----------|---------|
| **ChromeDriver не находится** | Установите Chrome или используйте `webdriver-manager` |
| **Ошибки импорта модулей** | Запускайте тесты из корневой папки проекта |
| **Docker не запускается** | Убедитесь, что Docker Desktop запущен |
| **Allure отчёт не генерируется** | Установите Allure командной строки |
| **Ошибка соединения с Selenium** | Проверьте, что Selenium Hub запущен в контейнере |

### Отладка тестов
```bash
# Подробный вывод с трассировкой
pytest src/tests/ -v --tb=long

# Запуск с отладочной информацией
pytest src/tests/ -v -s

# Запуск только упавших тестов
pytest --lf -v
```

---

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функциональности (`git checkout -b feature/amazing-feature`)
3. Зафиксируйте изменения (`git commit -m 'Add amazing feature'`)
4. Отправьте изменения в форк (`git push origin feature/amazing-feature`)
5. Создайте Pull Request

---

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. Подробнее см. в файле `LICENSE`.

---

## ✨ Достижения проекта

✅ **Полное соответствие ТЗ** - все требования реализованы  
✅ **Профессиональная архитектура** - Page Object, фикстуры, конфигурация  
✅ **Кроссплатформенность** - работает на Windows, Linux, macOS  
✅ **Готовность к CI/CD** - Docker, изолированное окружение, отчёты  
✅ **Документация** - подробные инструкции для новых участников  

---

## 📞 Контакты и поддержка

Если у вас возникли вопросы или проблемы:
1. Проверьте раздел [Устранение неполадок](#устранение-неполадок)
2. Создайте Issue в репозитории
3. Опишите проблему с максимальной детализацией

**Приятного тестирования!** 🚀

---

*Последнее обновление: Январь 2024*  
*Версия проекта: 0.10*  
*Статус: Активная разработка*
